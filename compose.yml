services:
  db:
    image: postgres:18
    environment:
      POSTGRES_USER: camille
      POSTGRES_PASSWORD: camille
      POSTGRES_DB: camille
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql

volumes:
  db_data:
