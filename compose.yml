services:
  pg18:
    image: "postgres:18"
    environment:
      POSTGRES_DB: "camille"
      POSTGRES_USER: "camille"
      POSTGRES_PASSWORD: "camille"
    volumes:
      - "pg_data:/var/lib/postgresql"
    networks:
      - default
  
  web:
    image: "killruana/camille:v6"
    environment:
      - "MATTERMOST_BASE_URL=https://mattermost.jtremesay.org"
      - "ALLOWED_HOSTS=alix.jtremesay.org"
      - "DATABASE_URL=postgres://camille:camille@pg18/camille"
      - "SALT_KEY_FILE=alix_salt_key"
      - "SECRET_KEY_FILE=alix_secret_key"
      - "LOGFIRE_TOKEN_FILE=alix_logfire_token"
    secrets:
      - "alix_mattermost_api_token"
      - "alix_salt_key"
      - "alix_secret_key"
      - "alix_logfire_token"
    networks:
      - default
      - traefik_public
    deploy:
      labels:
        - "traefik.enable=true"
        - "traefik.http.routers.alix.rule=Host(`alix.jtremesay.org`)"
        - "traefik.http.services.alix.loadBalancer.server.port=8000"

  mattermost:
    image: "killruana/camille:v6"
    command: "camille mattermost"
    environment:
      - "MATTERMOST_BASE_URL=https://mattermost.jtremesay.org"
      - "MATTERMOST_API_TOKEN_FILE=alix_mattermost_api_token"
      - "ALLOWED_HOSTS=alix.jtremesay.org"
      - "DATABASE_URL=postgres://camille:camille@pg18/camille"
      - "SALT_KEY_FILE=alix_salt_key"
      - "SECRET_KEY_FILE=alix_secret_key"
      - "LOGFIRE_TOKEN_FILE=alix_logfire_token"
    secrets:
      - "alix_mattermost_api_token"
      - "alix_salt_key"
      - "alix_secret_key"
      - "alix_logfire_token"
    networks:
      - default

networks:
  default:
  traefik_public:
    external: true

volumes:
  pg_data:

secrets:
  alix_mattermost_api_token:
    external: true
  alix_salt_key:
    external: true
  alix_secret_key:
    external: true
  alix_logfire_token:
    external: true
