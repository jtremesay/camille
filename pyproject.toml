[project]
name = "camille"
version = "0.1.0"
description = "An AI assistant for Mattermost"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "django>=5.2.1",
]

[build-system]
requires = ["setuptools"]
